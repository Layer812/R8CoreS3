[SEARCH]
gs=36
[REPLACE]
gs=24
[/]
[SEARCH]
   del(a,sq)
   b=gneis(sq)
   for nsq in all(b) do
    if nsq.dist==nil and chk(nsq) then
     nsq.dist=sq.dist+1
     add(na,nsq)
    end
   end
[REPLACE]
   del(a,sq)
   for di=0,3 do
    local nsq=gnei(sq,di)
    if nsq.dist==nil and chk(nsq) then
     nsq.dist=sq.dist+1
     add(na,nsq)
    end
   end
[/]
